# Repository conventions

- Use kebab-case for file and directory names, including Vue components and test files.
- Keep a test beside each production module using the `<module-name>.test.js` naming pattern.
